out_dir: "kissicp_results"

data:
    # deskew: true # use command line arg --deskew
    max_range: 80.0 # can be also changed in the CLI
    min_range: 3.0

mapping:
    # voxel_size: 0.4 # <- optional
    max_points_per_voxel: 20

adaptive_threshold:
    # fixed_threshold: 0.3 # <- optional, disables adaptive threshold
    initial_threshold: 2.0
    min_motion_th: 0.1

# registration:
#     max_num_iterations: 500 # <- optional
#     convergence_criterion: 0.0001 # <- optional
#     max_num_threads: 0 # <- optional, 0 means automatic
